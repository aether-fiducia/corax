use std::env;

use serenity::prelude::*;

//Build a simple struct to impl the EventHandler trait on
//That will serve the responses to the message event

struct Handler;

impl EventHandler for Event {

}

fn main() {

    println!("Working dev branch");

}
